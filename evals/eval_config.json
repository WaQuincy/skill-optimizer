{
  "version": 1,
  "description": "Simple v1 config: one scenario list shared by eval-only and optimize runs.",
  "scenario_ids": [
    "stuck_start",
    "already_has_next_step",
    "unrelated_handoff"
  ]
}

